# @cloudflare/next-on-pages khai peer dep next>=14.3.0 (thực tế là Next 15),
# nhưng dự án dùng Next 14.2.x (bản 14 mới nhất) và adapter vẫn chạy tốt.
# Bỏ qua kiểm tra peer để install được cả ở local lẫn build Git trên Cloudflare.
legacy-peer-deps=true
